import { describe ,it,test, expect } from 'vitest'

import { getRequest,deleteRequest,putRequest,postRequest } from '../../src/utils/api'

describe('api', () => {
    it('should get', () => {
        
    })
  })